# Backend
NODE_ENV=development
PORT=8080

# Gemini API Keys (supports multiple keys for load balancing)
# Single key: GEMINI_API_KEY=key1
# Multiple keys: GEMINI_API_KEY=key1,key2,key3
GEMINI_API_KEY=AIzaSyBTYxbfP89GtjBggTId6bEMnpiElHCyBsc,AIzaSyCL6YSYk3L5u_vtOb3Q1pnDSKg4yL1J5dM,AIzaSyDGg4kgd1xVn7AiDKvqD4rmC3T3QJR_hcQ

# Sticker output size (smaller = lower image tokens & faster)
# Common values: 512 / 768 / 1024 / 2K
GEMINI_STICKER_IMAGE_SIZE=1024

# Optional (recommended in deploy)
# REDIS_URL=redis://default:password@redis-host:6379
# ALLOWED_ORIGINS=https://sticker.example.com

# Admin (for internal stats endpoints)
# Provide in production, and keep it secret.
# ADMIN_TOKEN=change-me-to-a-long-random-string

# Usage metrics (optional)
# USAGE_UNIQUE_DEVICES_KEY=usage:unique_devices:v1

# Rate limiting (per instance)
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX=60

# Payment mode: 'free' (免费体验) or 'paid' (真实支付)
PAYMENT_MODE=free

# Free quota per day (only used when PAYMENT_MODE='free')
# 普通用户的每日免费次数
FREE_QUOTA_PER_DAY=3

# VIP Whitelist (optional)
# 格式: identifier1:quota1,identifier2:quota2
# identifier可以是: Device ID / IP / userId
# 示例: 让某个Device ID每天10次，某个IP每天5次
# VIP_WHITELIST=abc-123-def:10,192.168.1.100:5,::1_test-device:20
